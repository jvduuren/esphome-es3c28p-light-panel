# Copy to secrets.yaml and fill in. secrets.yaml is gitignored.
wifi_ssid: "YourNetwork"
wifi_password: "YourWifiPassword"

# Fallback hotspot the device raises if wifi fails
ap_password: "changeme12"

# ESPHome native API encryption key (32 bytes, base64).
# Regenerate with: openssl rand -base64 32
api_encryption_key: "REPLACE_ME"
